term "xterm-256color"
escape ^Jj

defbce on # erase background with current bg color
defscrollback 30000
startup_message off
vbell off

caption always "%{= wb} %-w%{=bu dr}%n %t%{-}%+w %= %{=b wk} [%l]%{=s wb} %Y/%m/%d %c:%s"


# 0
chdir "$HOME/workspace/"
screen -t ice

# 1
screen -t ice/s

# 2
screen -t logs

# 3
chdir "$HOME"
screen -t dev

# 4
screen -t home

# 5
chdir "$HOME/singapore"
screen -t singapore
select 0
