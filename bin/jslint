#!/bin/bash

JSL="/usr/local/bin/jsl"
PARAM="-conf $HOME/.bin/jsl.conf -nologo -nofilelisting -nosummary -nocontext"
if [ $1 = '-stdin' ]; then
    $JSL $PARAM $1
else
    len=$(pwd | wc -c)
    len=$(echo $len + 1 | bc)
    $JSL $PARAM -process $@ | cut -c $len"-"
fi

